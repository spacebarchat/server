version: '3.9'

services:
  fosscord:
    image: fosscord
    restart: unless-stopped
    build: .
    ports: [ '3001-3003:3001-3003' ]
    volumes: [ './:/srv/fosscord-server/' ]
    env_file: [ '.env' ]
